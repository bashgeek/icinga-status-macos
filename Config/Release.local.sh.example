# Copy to Release.local.sh and fill in your signing details.
ICINGA_RELEASE_TEAM_ID="${ICINGA_RELEASE_TEAM_ID:-YOUR_TEAM_ID}"
ICINGA_RELEASE_IDENTITY="${ICINGA_RELEASE_IDENTITY:-Developer ID Application: Your Organization ($ICINGA_RELEASE_TEAM_ID)}"
ICINGA_NOTARY_PROFILE="${ICINGA_NOTARY_PROFILE:-YOUR_NOTARY_PROFILE}"
